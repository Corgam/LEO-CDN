version: "3.7"
services:
  node2:
    build: ../FReD
    depends_on:
      - store2
    image: fred/fred:local
    container_name: node2
    entrypoint: "fred \
    --remote-storage-host 172.26.9.2:1337 \
    --peer-host 172.26.9.1:5555 \
    --nodeID node2 \
    --host 172.26.9.1:9001 \
    --cert /cert/node2.crt \
    --key /cert/node2.key \
    --ca-file /cert/ca.crt \
    --adaptor remote \
    --nase-host https://172.26.6.1:2379 \
    --nase-cert /cert/node2.crt \
    --nase-key /cert/node2.key \
    --nase-ca /cert/ca.crt \
    --handler dev \
    --badgerdb-path ./db \
    --remote-storage-cert /cert/node2.crt \
    --remote-storage-key /cert/node2.key  \
    --trigger-cert /cert/node2.crt \
    --trigger-key /cert/node2.key"
    environment:
      - LOG_LEVEL
    volumes:
      - ../cert/node2.crt:/cert/node2.crt
      - ../cert/node2.key:/cert/node2.key
      - ../cert/ca.crt:/cert/ca.crt
    ports:
      - 9005:9001
    networks:
      fredwork:
        ipv4_address: 172.26.9.1

  store2:
    build:
      context: ../FReD
      dockerfile: storage.Dockerfile
    image: fred/store:local
    container_name: store2
    entrypoint: "storageserver \
    --log-level '${LOG_LEVEL_STORE}' \
    --cert /cert/cert.crt \
    --key /cert/key.key \
    --ca-file /cert/ca.crt"
    volumes:
      - ../cert/store2.crt:/cert/cert.crt
      - ../cert/store2.key:/cert/key.key
      - ../cert/ca.crt:/cert/ca.crt
    networks:
      fredwork:
        ipv4_address: 172.26.9.2

networks:
  fredwork:
    external: true
